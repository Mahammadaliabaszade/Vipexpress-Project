@model List<Faq>
@{
    ViewData["Title"] = "Index";
    int count = 1;
}

<div class="row">

    <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body" style="width:800px">
                <h4 class="card-title">FAQ   table</h4>
                <a class="btn btn-success" asp-action="Create">Yeni sual cavab yaratmaq</a>
                <div class="table-responsive pt-3">
                    <table class="table table-bordered">
                        <thead>
                            <tr>
                                <th>
                                    #
                                </th>
                                <th>
                                    Sual
                                </th>

                                <th>
                                    Cavab
                                </th>

                            </tr>
                        </thead>
                        <tbody>
                            @foreach (Faq tar in Model)
                            {
                                <tr>
                                    <td>
                                        @count

                                    </td>
                                    <td>
                                        @tar.Question
                                    </td>
                                    <td>

                                        @tar.Answer
                                    </td>
                                    <td>

                                        <a class="btn btn-success" asp-action="Update" asp-route-id="@tar.Id"><i class="mdi mdi-auto-fix"> </i></a>
                                        <a class="btn btn-warning" asp-action="Delete" asp-route-id="@tar.Id"><i class="mdi mdi-close-box"> </i></a>


                                    </td>

                                </tr>
                                count++;
                            }


                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>